#!/usr/bin/env ruby
board=[" ", " "," ", " "," ", " "," ", " "," "]
puts "Welcome to Tic Tac Toe"

input = input.strip

index = input_to_index(input)

if valid_move(board, index)
  move(board, index)
else
  puts "Invalid move"
end
display_board(board)
turn(board, index)
